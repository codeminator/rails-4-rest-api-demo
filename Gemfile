source 'https://rubygems.org'

ruby '2.1.5'
gem 'rails', '4.1.8'

gem 'mysql2', '~> 0.3.18'					# Use mysql as the database for Active Record

group :test do
  gem 'rspec-rails'               # Handles testing
  gem 'fabrication'               # Generates fixtures for testing
  gem 'faker'                     # Generates fake values used in the fabricators
end

